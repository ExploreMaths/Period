-- Text utilities.

define join with list of string parts, string sep returns string:
    if (length with parts) == 0, then:
        return "".
    let result be parts[0].
    let i be 1.
    while i < (length with parts) repeat:
        set result to result + sep + parts[i].
        set i to i + 1.
    return result.
