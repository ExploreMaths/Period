"""Built-in Period standard library modules."""
from . import io, json, math, os, random, string, time

BUILTIN_MODULES = {
    "io": io,
    "json": json,
    "math": math,
    "os": os,
    "random": random,
    "string": string,
    "time": time,
}
